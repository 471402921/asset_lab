node_modules/
.DS_Store
.vscode/
tmp/
.mcp.json
*.log

# pixellab 原始导出: workflow buffer, 不进 git
temporary_asset/*
!temporary_asset/.gitkeep

.claude/*
# But Skills should ship with the repo (designer pulls → gets /asset-check etc.)
!.claude/skills/

# TLS material for the deploy box (generated on remote, never tracked)
cert.pem
key.pem
